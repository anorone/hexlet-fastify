extends ../layouts/index

block content
  div
    a(href=route('newUser')) Create user
  if users.length === 0
    p No users
  else
    ul
      each user in users
        li
          a(href=route('user', { userId: user.id }))= user.name
